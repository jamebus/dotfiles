install
.oh-my-zsh/cache
{{- if not (lookPath "pag") }}
.pagpapanatili
{{- end }}
{{- if not (lookPath "ayusin") }}
.ayusin
{{- end }}
{{- if not (lookPath "terraform") }}
.terraformrc
.terraform.d
{{- end }}
{{- if or (not (lookPath "git")) (eq (env "REMOTE_CONTAINERS") "true") }}
.gitconfig
{{- end }}
{{- if not (lookPath "git") }}
.gitignore-global
{{- end }}
